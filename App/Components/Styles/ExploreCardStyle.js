import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    padding: 10,
    borderRadius: 2,
    backgroundColor: "white",
    flex: 1,
    marginBottom: 10,
  }
});
